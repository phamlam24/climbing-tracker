# Copy this to .env and set a strong secret token.
# When deployed, visit /?admin=<your-token> to unlock admin mode.
# On localhost, admin mode is always active.
ADMIN_TOKEN=change-me-to-something-secret
