---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className = '' } = Astro.props;
---

<div class={`lp-card p-5 ${className}`}>
  <p class="lp-eyebrow mb-3">
    {label}
  </p>
  <slot />
</div>
